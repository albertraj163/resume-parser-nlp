# Resume Parser (NLP)
A minimal demo to extract basic fields from a plain-text resume using regex and spaCy.

## Files
- `sample_resume.txt` - example resume text
- `resume_parser.py` - simple parser script
- `requirements.txt` - dependencies

## How to run
```bash
pip install -r requirements.txt
python resume_parser.py
```
